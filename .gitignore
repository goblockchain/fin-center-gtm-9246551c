# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
.vite
*.local
*.tsbuildinfo

# env
# .env guarda SÓ valores PUBLICÁVEIS (URL + anon/publishable key) — pode ser versionado.
# Segredos reais (service_role/secret) NUNCA: ponha em .env.local (já coberto por *.local).
.env.*
!.env.example

# editor / os
.claude/settings.local.json
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# logs
logs
*.log
npm-debug.log*

# supabase local
supabase/.branches
supabase/.temp

# arquivo de conveniência (concatenação para colar no SQL Editor)
supabase/SETUP_CLOUD.sql
